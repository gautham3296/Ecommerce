/**
 * Hostinger Production Entry Point
 * 
 * Hostinger's hPanel launches the application through this root file.
 * It loads and executes the bundled, compiled production server.
 */

import "./dist/server.cjs";
